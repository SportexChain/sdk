export function buyStake(team: any, amount: number) {
  console.log(`Bought ${amount} stake in ${team.name}`);
}

export function sellStake(team: any, amount: number) {
  console.log(`Sold ${amount} stake in ${team.name}`);
}
